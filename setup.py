from setuptools import setup, find_packages

setup(
    name="pydobby",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Ahmed Abdella",
    description="A Python web server framework",
    python_requires=">=3.6",
)
